import { FormLabelForDirective } from './form-label-for.directive';

describe('FormLabelForDirective', () => {
  it('should create an instance', () => {
    const directive = new FormLabelForDirective();
    expect(directive).toBeTruthy();
  });
});
